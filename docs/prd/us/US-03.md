## US-03：创建与配置 Tool

### 使用场景

管理员进入「Tools」Tab，查看和配置两类工具：

- **预置工具（3 种）**：系统内置的 `SEARCH_METADATA`、`GET_TABLE_INFO`、`EXECUTE_SQL`，所有服务共享同样的名称、描述和输入参数定义。管理员只需决定开关和调参（如 `EXECUTE_SQL` 的权限策略、超时、行数限制）。预置工具不可删除、不可改名。
- **自定义工具**：用户创建的参数化 SQL 工具（`PARAMETERIZED_SQL`），每个服务可创建多个。管理员自行定义名称、描述、SQL 模板、参数和权限。

`annotations` 和 `inputSchema` 由 MCP 协议适配层按需生成，管理端不展示也不存储。

### 交互流程

```
Tools Tab
  ├─ 预置工具区
  │    ├─ 卡片列表（每行一个预置工具）：
  │    │    ├─ 信息：名称、描述摘要、类型标签
  │    │    ├─ 启用开关（即时生效）
  │    │    └─ EXECUTE_SQL 额外 [配置] 按钮 → 权限配置弹窗
  │    │
  │    └─ 权限配置弹窗（仅 EXECUTE_SQL）：
  │         ├─ 复选框组：SELECT / INSERT / UPDATE / DELETE / DDL / 多语句
  │         │    └─ SELECT 默认勾选，其余默认不勾选
  │         ├─ 最大返回行数（数字输入，默认 1000）
  │         ├─ 超时时间（秒，数字输入，默认 30）
  │         └─ 是否需要确认（开关）
  │
  ├─ 自定义工具区
  │    ├─ 工具列表表格：
  │    │    ├─ 列：名称、描述（截断）、启用开关、操作（编辑/删除/测试）
  │    │    └─ [新建参数化 SQL 工具] 按钮
  │    │
  │    └─ 编辑表单：
  │         ├─ 通用配置：
  │         │    ├─ name（校验格式 + 唯一性）
  │         │    ├─ title（可选显示名）
  │         │    ├─ description（必填，帮助 LLM 理解用途）
  │         │    └─ 启用开关
  │         │
  │         ├─ 数据源选择（下拉，限定为 data_scope 内的数据源）
  │         │
  │         ├─ SQL 模板编辑器（Textarea，等宽字体）
  │         │
  │         ├─ 参数列表编辑器：
  │         │    └─ 动态表格：[参数名 / 类型（String/Number/Boolean/Date/Array）/ 必填 / 默认值 / 描述]
  │         │
  │         ├─ 权限配置：
  │         │    ├─ 复选框组：SELECT / INSERT / UPDATE / DELETE / DDL / 多语句
  │         │    ├─ 最大返回行数
  │         │    ├─ 超时时间
  │         │    └─ 是否需要确认
  │         │
  │         └─ Annotations 预览（只读灰色卡片，纯前端根据 permConfig 计算）

调试测试：在列表中点击 [测试] → 见 US-07
```

### 异常与边界

- name 格式不合法：实时校验提示「仅允许 A-Z, a-z, 0-9, _, -, .，1-128 字符」
- name 重复：保存时后端校验，返回「Tool 名称已被使用」（仅自定义工具；预置工具无此概念）
- 预置工具不可删除、不可改名
- PARAMETERIZED_SQL 的 SQL 模板语法错误：保存时不做语法校验（不同数据库方言不同），执行时报错
- PARAMETERIZED_SQL 参数名未在 SQL 模板中引用：保存时前端 warning 提示（非阻止）
- 删除自定义工具：确认弹窗，提示「删除后 MCP Client 将无法调用此工具」
- 已发布服务修改 Tool：保存到草稿副本，需重新发布

### 后端接口

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/v1/mcp-services/{id}/tools` | 全量列表，按预置/自定义分组返回 |
| `PUT` | `/v1/mcp-services/{id}/tools/{toolId}` | 更新工具（含 enabled 开关） |
| `POST` | `/v1/mcp-services/{id}/tools` | 创建自定义工具 |
| `DELETE` | `/v1/mcp-services/{id}/tools/{toolId}` | 删除自定义工具 |
| `POST` | `/v1/mcp-services/{id}/tools/{toolId}/test` | 调试执行（US-07 实现） |

`toolId` 取值：
- 预置工具：`SEARCH_METADATA`、`GET_TABLE_INFO`、`EXECUTE_SQL`
- 自定义工具：DB 生成的 UUID

**列表响应结构（分组）：**
```json
{
  "prebuilt": [
    {
      "id": "SEARCH_METADATA",
      "tool_type": "SEARCH_METADATA",
      "name": "search_metadata",
      "description": "Search metadata across data sources",
      "enabled": true,
      "config": { "timeout": 30 }
    },
    {
      "id": "GET_TABLE_INFO",
      "tool_type": "GET_TABLE_INFO",
      "name": "get_table_info",
      "description": "Get detailed table and column information",
      "enabled": true,
      "config": { "timeout": 30 }
    },
    {
      "id": "EXECUTE_SQL",
      "tool_type": "EXECUTE_SQL",
      "name": "execute_sql",
      "description": "Execute SQL query against a data source",
      "enabled": true,
      "config": {
        "perm_config": { "allow_select": true, "allow_insert": false, "allow_update": false, "allow_delete": false, "allow_ddl": false, "allow_multi": false },
        "timeout": 30,
        "max_rows": 1000,
        "confirm_required": false
      }
    }
  ],
  "custom": [
    {
      "id": "uuid-001",
      "tool_type": "PARAMETERIZED_SQL",
      "name": "list_tasks",
      "title": "查询任务列表",
      "description": "按状态筛选所有任务",
      "enabled": true,
      "config": {
        "data_source_id": "ds-001",
        "sql_template": "SELECT * FROM tasks WHERE status = :status",
        "parameters": [
          { "name": "status", "type": "String", "required": true, "default_value": null, "description": "任务状态" }
        ],
        "perm_config": { "allow_select": true, "allow_insert": false, "allow_update": false, "allow_delete": false, "allow_ddl": false, "allow_multi": false },
        "timeout": 30,
        "max_rows": 1000,
        "confirm_required": false
      }
    }
  ]
}
```

### 前端设计

- 接口返回分组数据（`prebuilt` + `custom`），前端直接按区域渲染，无需判别字段
- 预置工具区：只读卡片行，仅开关 + EXECUTE_SQL 的配置按钮；不可删除、不可改名
- 自定义工具区：标准表格 + 新建按钮（单一入口，不再需要类型选择下拉）
- 新建/编辑弹窗为 Modal，字段较多可用分段折叠（通用 → SQL/参数 → 权限）
- SQL 模板编辑器：等宽字体、语法无高亮（V1）
- Annotations 预览：纯前端根据 `perm_config` 计算，展示在弹窗中供参考
- 启用开关即时生效：前端直接调 PUT 更新 `enabled`

### 数据库设计

**`mcp_prebuilt_tool_config` 表**（预置工具 per-service 配置）：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | VARCHAR | 主键 |
| `service_id` | VARCHAR | FK → mcp_service.id |
| `tool_type` | VARCHAR(30) | SEARCH_METADATA / GET_TABLE_INFO / EXECUTE_SQL |
| `enabled` | BOOLEAN | 是否启用，默认 true |
| `config` | TEXT(JSON) | 配置对象，null 时使用枚举中的默认值 |
| 审计字段 | — | 继承 BasePO |

索引：`UNIQUE(service_id, tool_type)`

**`mcp_custom_tool` 表**（自定义工具）：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | VARCHAR | 主键 |
| `service_id` | VARCHAR | FK → mcp_service.id |
| `name` | VARCHAR(128) | Tool 标识（服务内唯一，格式 [A-Za-z0-9_.-]{1,128}） |
| `title` | VARCHAR(255) | 显示名（可选） |
| `description` | TEXT | 功能描述 |
| `enabled` | BOOLEAN | 是否启用 |
| `config` | TEXT(JSON) | `{ data_source_id, sql_template, parameters[], perm_config, timeout, max_rows, confirm_required }` |
| 审计字段 | — | 继承 BasePO |

索引：`UNIQUE(service_id, name)`，`(service_id)`

### 预置工具默认值（代码中定义）

| 工具 | name | description | 默认 config |
|------|------|-------------|-------------|
| SEARCH_METADATA | `search_metadata` | 系统定义 | `{ timeout: 30 }` |
| GET_TABLE_INFO | `get_table_info` | 系统定义 | `{ timeout: 30 }` |
| EXECUTE_SQL | `execute_sql` | 系统定义 | `{ perm_config: {allow_select:true, 其余false}, timeout: 30, max_rows: 1000, confirm_required: false }` |

无 DB 记录时（新服务）：默认全部启用，使用代码中的默认配置。

### Annotations 映射规则

MCP 协议适配层在生成 `tools/list` 时计算，管理端 API 不返回。映射规则：

| 权限组合 | 生成的 annotations |
|----------|-------------------|
| 仅允许 SELECT | `readOnlyHint: true, destructiveHint: false, idempotentHint: true` |
| 允许 INSERT / UPDATE / DELETE | `destructiveHint: true, readOnlyHint: false` |
| 允许 DDL | `destructiveHint: true` |

SEARCH_METADATA 和 GET_TABLE_INFO 无权限配置，固定为 `{ readOnlyHint: true, destructiveHint: false, idempotentHint: true }`。

### 安全设计

- 自定义 Tool 创建时，数据源选择列表限定为 data_scope 内的数据源
- 执行时由执行引擎进一步校验数据范围

### 验收标准

1. 新服务进入 Tools Tab，3 个预置工具默认显示为已启用
2. 关闭 SEARCH_METADATA，刷新页面后保持关闭状态
3. 配置 EXECUTE_SQL 权限为 SELECT + INSERT，前端 annotations 预览显示 `readOnlyHint: false, destructiveHint: true`
4. 创建自定义工具，name 格式不合法时实时提示
5. 创建同名自定义工具时报错「Tool 名称已被使用」
6. 删除自定义工具，确认弹窗展示后执行，列表不再显示
