## US-06：管理服务 Token

> **状态：V1 暂缓实现**
>
> 认证鉴权统一在应用层（Spring Security / API Gateway）处理，MCP 构建模块不管理服务级 Token。外部 MCP Client 通过应用层的认证机制（如 API Key、OAuth、IP 白名单等）访问 MCP endpoint，不在 MCP 模块内单独设计和实现 Token 管理功能。
>
> 后续若有明确的多租户隔离、调用者身份透传等场景，再评估是否在 MCP 模块引入独立的认证机制。

---

