## US-04：创建与配置 Resource

> **状态：V1 暂缓实现**
>
> V1 定义的两种 Resource（`examples` 和 `sql-policy`）在 MCP 协议场景中实际价值不足：
> - `sql-policy` 与 Tool annotations 信息重复，annotations 是协议标准方式
> - `examples` 是静态文本，Agent 应走 Tool 链路动态生成 SQL
>
> 待后续版本有明确场景（如将数据源 schema 注册为 Resource 供 Agent 读取）时重新评估。
