# 系统配置 - 端到端测试

## TC-SYS-001 获取系统配置
**级别：** P1
**前置：** 无

1. 获取系统配置（GET /v1/system/config）
2. 验证返回结果：
   - 状态码 200
   - 响应体非空
   - 包含系统配置相关的字段（如 column-value-sample-limit 等）
   - 每个配置项的值合理（非 null、非异常值）
3. 无需认证即可访问（如果该接口不需要认证）
