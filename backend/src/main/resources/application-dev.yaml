spring:
  datasource:
    driver-class-name: org.h2.Driver
#    url: jdbc:h2:mem:dataconnai;DATABASE_TO_UPPER=false;QUERY_TIMEOUT=30
    url: jdbc:h2:file:./db/dataconnai;QUERY_TIMEOUT=30
    username: root
    password: dca
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      path: /h2-console/semantic
      enabled: true
  jackson:
    property-naming-strategy: SNAKE_CASE